name: Apply geometry metrics patch

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Patch application is temporarily paused while transfer checksums are repaired."
